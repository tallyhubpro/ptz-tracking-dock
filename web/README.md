# HTML Alternative UI

This folder contains the browser-based PTZ control UI alternative:

- `PTZ Camera Tracking OBS.html`

Use this when you want a web-driven control surface instead of the native OBS plugin.

Notes:
- Browser hotkeys only work when the page has focus.
- Browser/CORS behavior depends on OBS Browser Source and camera endpoint support.
